import styled from "styled-components";

export const HiddenInput = styled.input`
  clip: rect(1px, 1px, 1px, 1px);
  height: 1px;
  left: 0;
  overflow: hidden;
  position: absolute;
  top: 0;
  width: 1px;
`;
